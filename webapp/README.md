Daily Update Web App

A simple web application generated from screenshots that lets you update content every day.

How to run:
1. Open the project folder in a terminal.
2. Run `npx serve` or open `index.html` in a browser.

This is a starting scaffold. Update styles and content as needed.
